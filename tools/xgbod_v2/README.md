# pre_xgbod (XGBOD) — artifact Dockerfile

**Upstream:** https://github.com/yzhao062/XGBOD  
**Pinned commit:** `8017b56e3b9fe115901c910037366f27606ab611` (`upstream/master` at patch generation time)

**Landseer delta:** `patches/landseer-on-upstream.patch` (full diff from upstream to our fork’s `HEAD`)

Build:

```bash
docker build -f Dockerfile-main -t pre_xgbod:artifact .
```

