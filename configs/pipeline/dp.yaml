dataset:
  name: cifar10
  variant: clean
model:
  script: configs/model/config_model_resnet20_dp.py
  framework: pytorch
pipeline:
  pre_training:
    tools:
      - pre_noop
  during_training:
    tools:
      - in_dp_celeba
      - in_noop
  post_training:
    tools:
      - post_noop
  deployment:
    tools:
      - deploy_noop 