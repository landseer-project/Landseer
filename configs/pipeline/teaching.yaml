dataset:
  name: cifar10
  variant: clean
model:
  script: configs/model/config_model_combined.py
  framework: pytorch
pipeline:
  pre_training:
    tools:
      - pre_xgbod
      - pre_noop
  during_training:
    tools:
      - in_teaching
      - in_noop
  post_training:
    tools:
      - post_fineprune
      - post_noop
  deployment:
    tools:
      - deploy_dp
      - deploy_noop